apiVersion: v1
kind: Service
metadata:
  name: {{ include "mychartwithdep.fullname" . }}
  labels:
    {{- include "mychartwithdep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mychartwithdep.selectorLabels" . | nindent 4 }}
