apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mychartwithdep.fullname" . }}-test-connection"
  labels:
    {{- include "mychartwithdep.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mychartwithdep.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
